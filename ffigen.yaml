# Run with `dart run ffigen --config ffigen.yaml`.
name: LlxFlutterBindings
description: |
  Bindings for `src/llx_flutter.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/llx_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/llx_flutter.h'
  include-directives:
    - 'src/llx_flutter.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
